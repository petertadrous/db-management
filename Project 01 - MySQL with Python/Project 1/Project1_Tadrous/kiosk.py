class kiosk:
    # create a kiosk object for the specified kiosk
    def __init__(self, kid, addr1, addr2, city, stat, zip, capacity):
        self.kid = kid
        self.addr1 = addr1
        self.addr2 = addr2
        self.city = city
        self.stat = stat
        self.zip = zip
        self.capacity = capacity

    
